server:
  port: 8080

spring:
  application:
    name: Web Application built to report Sales and Products Data

ribbon:
  ServerListRefreshInterval: 1000

endpoints:
  health:
    sensitive: false
  restart:
    enabled: true
  shutdown:
    enabled: true